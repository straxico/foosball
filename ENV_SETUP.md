# 🔐 راهنمای تنظیم متغیرهای محیطی

این پروژه برای کار کردن نیاز به اتصال به Supabase دارد. برای تنظیم محیط توسعه محلی خود، مراحل زیر را دنبال کنید:

## 📋 مراحل تنظیم

### 1. ایجاد حساب Supabase

اگر هنوز حساب کاربری ندارید:
- به [supabase.com](https://supabase.com) بروید
- روی "Start your project" کلیک کنید
- با ایمیل یا GitHub ثبت‌نام کنید

### 2. ایجاد پروژه جدید

1. پس از ورود، روی "New Project" کلیک کنید
2. نام پروژه را وارد کنید (مثلاً: `foosball-tournament`)
3. یک پسورد قوی برای دیتابیس انتخاب کنید
4. منطقه (Region) نزدیک به خودتان را انتخاب کنید
5. روی "Create new project" کلیک کنید
6. صبر کنید تا پروژه ایجاد شود (حدود 2 دقیقه)

### 3. دریافت کلیدهای API

1. در پنل پروژه، از منوی سمت چپ به `Settings` بروید
2. روی `API` کلیک کنید
3. در بخش "Project API keys" دو مقدار مهم را خواهید دید:
   - **Project URL**: آدرس API پروژه شما
   - **anon/public key**: کلید عمومی برای دسترسی

### 4. تنظیم فایل محیطی

1. فایل `.env.local` را در ریشه پروژه باز کنید
2. مقادیر را به این صورت پر کنید:

```env
VITE_SUPABASE_URL=https://xxxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

⚠️ **مهم:** 
- این مقادیر را در هیچ‌جا commit نکنید
- فایل `.env.local` در `.gitignore` قرار دارد
- هرگز `service_role` key را در frontend استفاده نکنید

### 5. راه‌اندازی دیتابیس

1. در پنل Supabase، از منوی چپ به `SQL Editor` بروید
2. روی "New query" کلیک کنید
3. محتویات فایل `setup.sql` را کپی کرده و در ویرایشگر paste کنید
4. روی "Run" کلیک کنید تا جداول ایجاد شوند

### 6. تست اتصال

```bash
npm run dev
```

اگر همه چیز درست تنظیم شده باشد، برنامه باید بدون خطا اجرا شود.

## 🔧 عیب‌یابی

### خطای "Supabase URL and anon key are required"

✅ **راه حل:**
- مطمئن شوید فایل `.env.local` در ریشه پروژه وجود دارد
- مطمئن شوید نام متغیرها دقیقاً `VITE_SUPABASE_URL` و `VITE_SUPABASE_ANON_KEY` است
- سرور dev را restart کنید (`Ctrl+C` و دوباره `npm run dev`)

### خطاهای احراز هویت

✅ **راه حل:**
- در پنل Supabase به `Authentication` > `Providers` بروید
- مطمئن شوید "Email" فعال است
- در `Authentication` > `URL Configuration` آدرس `http://localhost:5173` را اضافه کنید

### خطاهای دیتابیس

✅ **راه حل:**
- مطمئن شوید فایل `setup.sql` به طور کامل اجرا شده
- در `Table Editor` بررسی کنید که جداول ایجاد شده‌اند
- بررسی کنید که RLS (Row Level Security) policies به درستی تنظیم شده‌اند

## 📚 منابع بیشتر

- [مستندات Supabase](https://supabase.com/docs)
- [راهنمای Vite Environment Variables](https://vitejs.dev/guide/env-and-mode.html)
- [مستندات Supabase Auth](https://supabase.com/docs/guides/auth)

## 🆘 کمک بیشتر

اگر مشکلی حل نشد، Issue جدید در GitHub ایجاد کنید.
