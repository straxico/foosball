# ⚽ مدیریت مسابقات فوتبال دستی

یک برنامه جامع برای مدیریت تورنمنت‌های فوتبال دستی با قابلیت پیش‌بینی نتایج و جدول امتیازات.

## ✨ ویژگی‌ها

- 🏆 جدول لیگ با آمار کامل تیم‌ها
- 📅 مدیریت برنامه بازی‌ها
- 🎯 سیستم پیش‌بینی نتایج
- 🥇 جدول برترین پیش‌بینی‌کنندگان
- 👤 سیستم احراز هویت کاربران
- 👑 پنل ادمین برای مدیریت
- 📱 طراحی ریسپانسیو و مدرن

## 🚀 نصب و راه‌اندازی

### پیش‌نیازها

- Node.js (نسخه 18 یا بالاتر)
- حساب کاربری Supabase

### مراحل نصب

1. **کلون کردن پروژه:**
   ```bash
   git clone <repository-url>
   cd foosball
   ```

2. **نصب وابستگی‌ها:**
   ```bash
   npm install
   ```

3. **تنظیم متغیرهای محیطی:**
   
   فایل `.env.local` را کپی کرده و مقادیر را پر کنید:
   ```bash
   cp .env.example .env.local
   ```
   
   سپس فایل `.env.local` را باز کرده و مقادیر زیر را از پنل Supabase خود وارد کنید:
   
   ```env
   VITE_SUPABASE_URL=https://your-project.supabase.co
   VITE_SUPABASE_ANON_KEY=your-anon-key-here
   ```
   
   **چگونه این مقادیر را پیدا کنیم؟**
   - به [app.supabase.com](https://app.supabase.com) بروید
   - پروژه خود را باز کنید
   - از منوی چپ، به `Settings` > `API` بروید
   - `Project URL` را کپی کرده و در `VITE_SUPABASE_URL` قرار دهید
   - `anon/public key` را کپی کرده و در `VITE_SUPABASE_ANON_KEY` قرار دهید

4. **راه‌اندازی دیتابیس Supabase:**
   
   فایل `setup.sql` را در SQL Editor پنل Supabase اجرا کنید تا جداول و ساختار دیتابیس ایجاد شود.

5. **اجرای برنامه:**
   ```bash
   npm run dev
   ```
   
   برنامه در آدرس `http://localhost:5173` در دسترس خواهد بود.

## 📦 ساختار پروژه

```
foosball/
├── components/          # کامپوننت‌های React
│   ├── Auth.tsx        # فرم ورود/ثبت‌نام
│   ├── Header.tsx      # سربرگ و منوی اصلی
│   ├── LeagueTable.tsx # جدول لیگ
│   ├── MatchSchedule.tsx   # برنامه بازی‌ها
│   ├── PredictionView.tsx  # صفحه پیش‌بینی
│   ├── PredictionLeaderboard.tsx # جدول برترین‌ها
│   └── TeamDetails.tsx     # جزئیات تیم
├── services/           # سرویس‌های دیتابیس
│   └── supabaseService.ts
├── types.ts           # تعریف انواع TypeScript
├── supabaseClient.ts  # کلاینت Supabase
├── App.tsx            # کامپوننت اصلی
└── setup.sql          # اسکریپت راه‌اندازی دیتابیس
```

## 🛠️ تکنولوژی‌های استفاده شده

- **React 19** - کتابخانه UI
- **TypeScript** - برای type safety
- **Vite** - ابزار build
- **Supabase** - Backend و دیتابیس
- **Tailwind CSS** - استایل‌دهی
- **Vazirmatn Font** - فونت فارسی

## 👥 نقش‌های کاربری

### کاربر عادی
- مشاهده جدول لیگ و آمار تیم‌ها
- مشاهده برنامه بازی‌ها
- ثبت پیش‌بینی برای بازی‌های آینده
- مشاهده جدول برترین‌ها
- مشاهده امتیاز خود

### ادمین
تمام دسترسی‌های کاربر عادی به علاوه:
- ویرایش نام تیم‌ها
- ثبت/ویرایش نتایج بازی‌ها
- تغییر تاریخ بازی‌ها

## 📝 راهنمای استفاده

### برای کاربران

1. **ثبت‌نام/ورود:** از دکمه "ورود / ثبت‌نام" در بالای صفحه استفاده کنید
2. **مشاهده جدول لیگ:** تب "جدول لیگ" را انتخاب کنید
3. **پیش‌بینی:** به تب "پیش‌بینی" رفته و نتیجه بازی‌ها را پیش‌بینی کنید
4. **مشاهده رتبه:** در تب "برترین‌ها" رتبه خود را ببینید

### برای ادمین‌ها

1. نقش ادمین باید در دیتابیس تنظیم شود
2. پس از ورود، گزینه‌های مدیریتی در دسترس خواهد بود
3. برای ثبت نتیجه: به تب "برنامه بازی‌ها" رفته و نتایج را وارد کنید

## 🎨 ویژگی‌های طراحی

- طراحی مدرن با گرادیانت‌های زیبا
- انیمیشن‌های smooth و جذاب
- Responsive برای تمام دستگاه‌ها
- Dark theme حرفه‌ای
- استفاده از ایموجی‌ها برای بهبود UX
- افکت‌های hover و transition جذاب

## 📄 لایسنس

این پروژه تحت لایسنس MIT منتشر شده است.

## 🤝 مشارکت

برای مشارکت در این پروژه:
1. Fork کنید
2. یک branch جدید بسازید
3. تغییرات خود را commit کنید
4. Push کنید و Pull Request ایجاد کنید

## 📞 پشتیبانی

در صورت بروز مشکل یا سوال، Issue جدید در GitHub ایجاد کنید.
